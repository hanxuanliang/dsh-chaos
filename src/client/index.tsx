// Placeholder client: the previous collaboration frontend was removed wholesale
// and is being rebuilt from scratch. Registers nothing on purpose.

export const inject = [] as const

export function apply(): void {}
